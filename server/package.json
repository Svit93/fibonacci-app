{
  "name": "fibonacci-app-server",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node index.js",
    "buildAndStart": "npm install && tsc -p tsconfig.json && node index.js"
  },
  "type": "module",
  "dependencies": {
    "fastify": "^4.28.0",
    "fibonacci-app-server": "file:"
  },
  "devDependencies": {
    "@types/node": "^20.14.3",
    "fastify-cli": "^6.2.1",
    "typescript": "^5.4.5"
  }
}
